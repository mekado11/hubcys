/**
 * generateGrandSummary — Cloud Function stub.
 * Replace this with a real Firebase Cloud Function call or Vercel API route.
 */
export const generateGrandSummary = async (params) => {
  throw new Error(`generateGrandSummary is not yet implemented. Wire up a backend function or API route.`);
};
