/**
 * ipInsight — Cloud Function stub.
 * Replace this with a real Firebase Cloud Function call or Vercel API route.
 */
export const ipInsight = async (params) => {
  throw new Error(`ipInsight is not yet implemented. Wire up a backend function or API route.`);
};
