/**
 * fetchCisaKev — Cloud Function stub.
 * Replace this with a real Firebase Cloud Function call or Vercel API route.
 */
export const fetchCisaKev = async (params) => {
  throw new Error(`fetchCisaKev is not yet implemented. Wire up a backend function or API route.`);
};
